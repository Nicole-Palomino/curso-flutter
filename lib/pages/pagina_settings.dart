import 'package:flutter/material.dart';

class PaginaSettings extends StatelessWidget {
  const PaginaSettings({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Settings', style: TextStyle(fontSize: 30),),
    );
  }
}